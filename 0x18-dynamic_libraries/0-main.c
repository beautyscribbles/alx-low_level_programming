#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point to check the code
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
	printf("%d\n", _strlen("My Dyn Lib"));
	return (EXIT_SUCCESS);
}
