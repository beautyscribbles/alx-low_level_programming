#include "arithmatic.h"
/**
 *add - function that add two integer
 *@a: takes value of a
 *@b: takes the value of b
 *Return: return the sum of two integer
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 *add - function that subtwo integer
 *@a: takes value of a
 *@b: takes the value of b
 *Return: sub of two integer
 */
int sub(int a, int b)
{
	return (a - b);
}

/**
 *add - function that multwo integer
 *@a: takes value of a
 *@b: takes the value of b
 *Return: mul of two integer
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 *add - function that div two integer
 *@a: takes value of a
 *@b: takes the value of b
 *Return: div of two integer
 */
int div(int a, int b)
{
	return (a / b);
}

/**
 *add - entry point of function
 *@a: inputs the value of a
 *@b: inputs the value of b
 *Return: mod of two integer
 */
int mod(int a, int b)
{
	return (a % b);
}
